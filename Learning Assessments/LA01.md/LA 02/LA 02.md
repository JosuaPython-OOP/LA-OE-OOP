```python
class MLHero():
    def __init__(self, name, role):
        self.name = name
        self.role = role

mage = MLHero("Kagura", "Mage")
marksman = MLHero("Lesley", "Marksman")

print(mage.name)
print(mage.role)
print(marksman.name)
print(marksman.role)
```python
